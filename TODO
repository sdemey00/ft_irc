- IRC Protocol ?

- Handle signals :
	- Need to send a closing server message ?
	- close all the client sockets
	- close own socket
	-> close all the connections in the server destructor

- Be able to handle multiple client connections
	- Not allowed to fork or thread -> non blocking calls -> ? while(1) on non blocking clients ?
	- poll, select, epoll ?

mmichele :
	- Processing recv :
		- When buffer size is big enough to make the buffer contain more than 
			one complete message in it.
			-> BUFFER_SIZE 1000
			-> /connect in irssi
		- The function doesn't see a complete message when it's split
			between two different partial buffers
			-> BUFFER_SIZE 2
			-> /connect in irssi
	- Make print memory function, to print the recv buffer.