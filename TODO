- IRC Protocol ?

- Handle signals :
	- Need to send a closing server message ?

mmichele :
	- Make print memory function, to print the recv buffer.
	- Does the servers sends a message to clients when it closes ?
	- Make tests withs MAX_CLIENTS macro